import React from 'react';
import ReactDOM from 'react-dom/client';
import { Frame } from './kg';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Frame />
  </React.StrictMode>
); 